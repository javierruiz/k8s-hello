apiVersion: v1
kind: Service

metadata:
  name: hello-apache

spec:
  type: {{ .Values.service.type }}

  selector:
    app: hello-apache

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80